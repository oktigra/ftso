# BACKLOG — сайт ФТСО (ftso67.ru)

## СОСТОЯНИЕ НА 03.09.2026 — один список, сверен с боем (main `e473f11`+)

Ниже — всё открытое на сегодня, в порядке к открытию приёма ПДн. Разделы старше
(«Состояние на 08.08.2026», блоки 0/A/B/C, «Порядок к запуску») сохранены ниже как
история решений: их галочки и «заглушки» устарели, верить этому списку.

**Закрыто, по замеру:** каркас всех разделов и CRUD (см. таблицу в `site/README.md`);
профиль игрока, срезы, парный разряд, фото (ТЗ ред. 6); DNS на VPS, HTTPS + HSTS;
юр. тексты ред. 02.09.2026; уведомление РКН подано 03.09.2026; приказ по ст. 22.1
от 09.08.2026 (Лазаренков И.С.); регистрационные документы на сайте; аудит WGR
96/100 закрыт кодом; `npm audit` прод-зависимостей — 0 (qs 6.16 через overrides);
приёмка 141/141; ритуал — общий трафарет в `orchestrator-sandbox`.

**Открыто — владелец (руками на сервере и в кабинетах):**
- [ ] Домен ftso67.ru оплачен до **16.11.2026** — продлить у RU-CENTER.
- [ ] Сменить пароль супер-админа. Права файлов — проверены 04.09.2026 на бою: `.env` 600, БД 600, storage 700, владелец ftso (уже были такими).
- [ ] SSH-ключ на root + отключить вход по паролю (fail2ban уже стоит).
- [x] ~~logrotate~~ — 04.09.2026: `/etc/logrotate.d/ftso` (pm2 daily/14, логи выката weekly/8, copytruncate) поставлен из origin/main, dry-run 0 ошибок; nginx ротирует пакетным конфигом daily/14 (PR #29).
- [ ] Офсайт-копия — решение владельца: «Аварийная копия» Timeweb 200 ₽/мес (тумблер на странице бэкапов сервера) или S3/скачивание. Локальная ступень закрыта 04.09 (см. «Закрыто»).
- [x] ~~Живой SMTP~~ — закрыто решением владельца 04.09.2026: пароль приложения остаётся в olegk333, под info@ не создаём; письма с сервера не идут, приём заявок и так выключен. Не поднимать.
- [ ] Договор-поручение с почтовым провайдером (ст. 6 ч. 3 152-ФЗ) — бумага.
- [ ] Проверить реестр операторов РКН живым браузером; появились — `INTAKE_ENABLED=1`.
- [ ] Наполнение через админку: справочники, новости, устав в библиотеку, первые
      турниры и игроки (первый живой `/player/:id` выкат не проверял).
- [ ] Тест движка рейтинга на реальном датасете федерации.

**Открыто — решения федерации (после них — код):**
- [ ] Правило возраста для среза: полные годы на сегодня (сейчас) или год рождения
      (календарь РТТ) — одна функция `ageOn` в `site/server/lib/age.mjs`.
- [ ] Набор возрастных срезов под региональный масштаб — список в `age.mjs`.
- [ ] Кнопка «согласие на распространение» в кабинете и второй чекбокс регистрации
      на витрину не влияют — оставить ради журнала или снести.
- [ ] Текст Политики: п. 2.7 Согласия в новой формулировке и ответственный по
      ст. 22.1 — редакция за чатом «Политика/фото».

**Открыто — код, следующие задачи:**
- [ ] Соцсети федерации — отдельная правка Согласия, потом код.
- [ ] Самостоятельная выгрузка своих данных по ст. 14 (не блокер).

**Закрыто 04.09.2026, по замеру — приёмка 148/148:**
- [x] Ежедневный бэкап на сервере: `deploy/backup.sh` (sqlite backup API + загрузки, tar.gz, 14 дней) и таймер `ftso-backup.timer` 03:30 МСК; установлен владельцем 04.09 (md5 сверены, первая копия 376 КБ, таймер active), копия `/root/backup.sh` самообновляется (PR #27). Пробный restore проверен на бою.
- [x] Реквизиты на `/contacts` рассыпались по букве (grid `1fr` + `overflow-wrap:anywhere`, длинный dt); переход темы 4.8 с → .45 с. Оба воспроизведены Chromium-тестами до фикса (PR #25).
- [x] Галерея на карточке турнира `/tournaments/:id`: секция «Фото» — снимки с `tournament_id` этого турнира, чужие не показываются (PR #26).

**Закрыто 03.09.2026 (сессия 4), по замеру — приёмка 145/145:**
- [x] `deploy-watch.sh`: дифф только из памяти (`sessions/`, `ritual/`, `NEXT_CHAT_START.md`)
      → fast-forward без рестарта pm2; функциональный тест в приёмке (PR #19).
- [x] Здоровье выката — `deploy/health.sh`: `/`, `/rating`, первый профиль из рейтинга
      (пока игроков нет — явный пропуск). `deploy-A.sh` внесён в репо (`deploy/deploy-A.sh`),
      копии `/root/deploy-watch.sh` и `/root/deploy-A.sh` подтягиваются из `origin/main` перед
      каждым выкатом — правка через PR доезжает сама (PR #20; первичная синхронизация сделана
      владельцем, md5 сверены).
- [x] РНИ игрока: `playerInput` → `INSERT/UPDATE` → поле в форме и таблице админки →
      `playerProfile` → тег «РНИ N» на профиле; пустое стирает, мусор отклоняется (PR #21).
- [x] Галерея соревнований (подп. 2 п. 1 ст. 152.1 ГК): `gallery_items.tournament_id`, выбор
      соревнования в админке, `/gallery/:id/image` инлайн (только растровые по сигнатуре,
      nosniff), сетка `<img>` с подписями, правовой блок + путь к удалению через контакты,
      подсказка секретарю про крупные портреты; EXIF-стрип доказан на профиле gallery (PR #22).

---

## Дополнение 03.09.2026 (вечер) — уведомление РКН подано, аудит WGR, документы федерации

- **Уведомление в РКН по ст. 22 подано 03.09.2026** (слово владельца). `INTAKE_ENABLED=1` — по записанному решению после появления ФТСО в реестре операторов (проверка реестра — живым браузером, из песочницы 503).
- **Приказ о назначении ответственного за организацию обработки ПДн (ст. 22.1) — есть, от 09.08.2026**: исполнительный директор Лазаренков И.С. Внесён в `legal.mjs` и на /contacts; в текст Политики — отдельной редакцией (чат «Политика/фото»).
- Реквизиты сверены по документам: свидетельство Минюста (ОГРН 1176733009243, адрес 214020), свидетельство ФНС (ИНН 6732145252, **КПП 673201001**), лист записи ЕГРЮЛ 22.04.2026 (президент Тимашков Р.А.), протокол № 07 от 03.04.2026. Карточка организации несёт индекс 214036 — расходится с ЕГРЮЛ, на сайте 214020.
- **Аудит WebGuardReport 03.09.2026: 96/100**, нарушений нет. Закрыто кодом: canonical, Open Graph, favicon, Permissions-Policy, robots.txt + sitemap.xml (живая, с профилями), llms.txt, JSON-LD организации, ОГРН/ИНН в подвале каждой страницы. `/admin` в аудите «200» — на деле 302 на /login (замер). Cookie-баннер не нужен: трекеров нет (реестр cookie в приёмке).
- [ ] **ДОМЕН ftso67.ru оплачен до 16.11.2026** (реестр доменов, аудит) — продлить у RU-CENTER заранее. Владелец.
- [x] Документы из ОТКРЫТЫХ реестров (свидетельство Минюста 2017, свидетельство ФНС ИНН/КПП 2017, лист записи ЕГРЮЛ 22.04.2026) — в коде `legal.mjs:PUBLIC_DOCUMENTS` + `site/public/docs`, на /documents и /federation (слово владельца 03.09.2026). Внутренние (приказ по ст. 22.1, протокол № 07, карточка с банковскими реквизитами) НЕ публикуются — при нужде через `/admin/library`. Устав — ещё не передан.

## Дополнение 03.09.2026 — публичный профиль по модели РТТ (ТЗ ред. 6, PR #9)

- `/player/:id` (числовой id): ФИО, город, пол, **возраст в полных годах**, группа, очки/место одиночный и парный, турниры, матчи с соперниками и партнёрами (ссылками), счётом, датой и турниром; фото из кабинета (`/player/:id/photo`, inline, no-cache + ETag). Обезличенный по ст. 21 → 404.
- `/rating`: строки → профиль; колонка «Возраст»; возрастные срезы как у РТТ (`site/server/lib/age.mjs`, один список), место внутри среза; фильтр разряда (парный — когда появятся данные). Главная ТОП-5 и карточка турнира — ссылками, счёт и дата матчей.
- МОДЕЛЬ ПУБЛИКАЦИИ ИЗМЕНЕНА: результаты публикуются на основании участия, согласие на распространение и 18-летие на витрину НЕ влияют. `anonymizeForPublic` знает только «Игрок удалён». Пункты ниже про «Скрыто по заявлению» и «публикация прекращается в день 18-летия» — история.
- Схема: `players.rni`; `results.discipline` (UNIQUE турнир+игрок+разряд); `matches.score`, `played_on`, `kind`, `winner_partner_id`, `loser_partner_id` (UNIQUE с `kind`). Миграция пересобирает обе таблицы, идемпотентна.
- Кабинет: подсказка у фото по §7 ТЗ, `POST /cabinet/photo/delete`; админка: `POST /admin/players/:id/photo/delete`, поля счёт/дата в форме матча. Приёмка 139/139.
- Открыто: правило возраста для среза (полные годы на сегодня vs год рождения — одна функция `ageOn`); кнопка согласия на распространение в кабинете и второй чекбокс регистрации на витрину не влияют — оставлены ради журнала.

## Состояние на 08.08.2026 (сверено с живым кодом, коммит 121022e)

Сайт РАЗВЁРНУТ на VPS ftso67 (http://194.87.187.42), но это КАРКАС: ядро работает, витрина почти пустая.

ГОТОВО (ядро — не трогать без причины):

- движок рейтинга (rating/) + matches (данные под тай-брейк есть);

- авторизация/сессии/CSRF; security-заголовки богатые (см. «ПРОВЕРЕНО»);

- главная по дизайну; рейтинг /rating (фильтры, экспорт CSV/Excel; PDF по КП — отдельно);

- админка за логином: CRUD игроков/турниров/результатов/матчей/пользователей, смена пароля, дашборд;

- action_log ПОЛНЫЙ; тексты /privacy /consent — рыба со структурой (оператор РФСОО «ФТСО» назван);

- СЛОЙ НЕСОВЕРШЕННОЛЕТНИХ (09.08.2026, PR #3): публикация в рейтинге прекращается в день 18-летия (основание от представителя отпало), возврат — собственным согласием мгновенно, движок не трогается; один действующий представитель на ребёнка, второй родитель — заменой через админку (решение заказчика, ч. 1 ст. 9); дата рождения видна и правится ТОЛЬКО в админке за логином, наружу не выводится; за участника младше 18 согласие даёт законный представитель; представитель — отдельная входная сущность (один логин на человека, дети списком), почта ребёнка под гейтом не собирается; переход в 18 конечным автоматом с заморозкой на +30; журнал согласий неизменяем на уровне СУБД (триггеры + ворота для ст. 21 и сроков); срок хранения данных представителя 3 года от снятия; COMPLIANCE.md — карта 152-ФЗ. Приёмка 123/123;

- инфра: Node 22 + pm2 (ftso, 127.0.0.1:3000), nginx :80 -> 3000, деплой из main, автозапуск.

Раскладка: приложение в site/ (код в site/server/*.mjs), движок в rating/ рядом — импорты НЕ ломать. npm из site/. БД пустая — наполняется через админку.

РЕКВИЗИТЫ (для юр-контента и раздела Контакты): оператор РФСОО «Федерация тенниса Смоленской области», 214020, г. Смоленск, ул. Попова, д. 31 (адрес БЕЗ «Смоленская область»); ОГРН 1176733009243, ИНН 6732145252; e-mail info@ftso67.ru; публичный ТЕЛЕФОН сайта 8-995-104-32-62 (как в ЕГРЮЛ).

## ЕДИНЫЕ ТРЕБОВАНИЯ К ЗАГРУЗКЕ ФАЙЛОВ (ко ВСЕМ аплоадам: документы турниров, галерея, документы /documents)

- проверка по magic bytes, не только по расширению/Content-Type; запрет исполняемых MIME;

- лимит размера; хранение ВНЕ webroot; отдача как attachment; ресайз для изображений.

## БЛОК 0 — СДЕЛАТЬ ПЕРВЫМ, до наполнения данными (Claude в чате + владелец)

Сервер уже в сети по IP:

- [ ] npm audit — закрыть высокие. Зависимости в проде = блокер, идёт ПЕРВЫМ (раньше SSH).

- [ ] Сменить пароль супер-админа (admin) — временный светился в чате.

- [x] Права на файлы VPS — проверено 04.09.2026: `.env` 600, БД 600, storage 700, владелец ftso.

- [ ] Этап 1b — SSH-ключ на root + отключить вход по паролю + fail2ban. ТОЛЬКО после проверки входа по ключу.

## A. Достройка публичной витрины (код вэб) — главный фронт работ

Юридический контент:

- [x] Политика /privacy — текст УЖЕ рыба (views/legal.ejs, оператор назван). Доделать: выверить реквизиты (см. выше) + СРОК хранения = «до отзыва согласия / выхода из федерации» (НЕ таймер-автоудаление БД) + права субъекта (ст.14-21) + РЕГЛАМЕНТ ОТВЕТА на обращение субъекта — 30 дней (ч.3 и ч.5 ст.21) + ответственный за организацию обработки (ст.22.1) + правовая проверка полноты. НЕ с нуля.
  СДЕЛАНО 08.08.2026: реквизиты вынесены в server/lib/legal.mjs (OPERATOR) — единый источник для юр-страниц, подвала и «Контактов»; редакция текстов = константа LEGAL_VERSION = '2026-08-08' (в журнал согласий кладётся эта строка, на странице печатается «Редакция от 08.08.2026»); тексты разведены по views/legal/{privacy,consent}.ejs, оболочка общая.

- [x] Согласие /consent — та же рыба (kind=consent). Выверить текст.
  СДЕЛАНО 08.08.2026: на странице ДВА САМОСТОЯТЕЛЬНЫХ согласия — на обработку (ст.9) и на РАСПРОСТРАНЕНИЕ (ст.10.1, оформляется отдельно по ч.6 ст.10.1: публикация ФИО в рейтинге общим согласием не покрывается). Якоря #processing и #distribution — на них ссылаются ДВА РАЗДЕЛЬНЫХ чекбокса формы регистрации. Прекращение распространения по требованию — 3 рабочих дня (ч.12 ст.10.1). Согласие на распространение ДОБРОВОЛЬНО: без него субъект обрабатывается, но не публикуется.
  ЖУРНАЛ СОГЛАСИЙ (следующий пункт) пишет ДВЕ РАЗДЕЛЬНЫЕ записи с одной LEGAL_VERSION — учесть в схеме.

- [x] Cookie-баннер — НЕ требуется: куки только сессионные (express-session), аналитики нет (grep пуст). Понадобится ТОЛЬКО при добавлении аналитики/рекламы.
  ПОДТВЕРЖДЕНО 08.08.2026: баннер НЕ требуется. Куки только сессионные (ftso.sid, express-session) — аутентификация и CSRF. Аналитики, рекламы и сторонних трекеров нет; браузерная проверка приёмки фиксирует НОЛЬ внешних запросов со страницы (шрифты самохост). Состав куки описан в разделе 8 Политики. Понадобится при добавлении аналитики — тогда же и пересмотреть.

- [x] Журнал согласий (регистрация игрока) — фиксация в БД: дата, IP, ВЕРСИЯ текста, что принято. RETENTION: срок хранения + автоочистка (запись = ПДн). Удаление записей согласия при праве на забвение (ст.21).
  СДЕЛАНО 08.08.2026: таблица consents (server/lib/consent-journal.mjs) — СОБЫТИЯМИ granted/revoked по двум видам (processing/distribution), с legal_version, source (web/offline), ip и датой. Отзыв = новая запись, не удаление.
  ФЛАГ players.is_public — производный от журнала (syncPlayerPublicFlag), дефолт 0. Пишется ТОЛЬКО через журнал; отсутствие поля в POST согласие НЕ трогает (отзыв не может случиться сам собой).
  ОБЕЗЛИЧИВАНИЕ — один слой на два случая (rating-service.anonymizeForPublic): нет согласия -> «Скрыто по заявлению», игрока нет в БД -> «Игрок удалён». МЕСТО И ОЧКИ СОХРАНЯЮТСЯ, движок считает по реальным данным, прячем только на выдаче. Город/пол/группа гасятся тоже -> через фильтры и поиск скрытого не опознать.
  RETENTION = CONSENT_RETENTION_DAYS (по умолчанию 1095 = 3 года от ОТЗЫВА); действующие согласия не чистятся никогда. Автоочистка при старте и раз в сутки.
  Приёмка: 5 новых проверок, всего 58/58.

Формы — ОБЩИЕ правила: только метод POST (никакого GET с ПДн); антиспам (honeypot/капча); явные состояния ошибок; при незавершённой заявке — сохранять ЧЕРНОВИК, не терять ввод; email-уведомления с FALLBACK при сбое отправки (не молча).

Три функции за мёртвыми кнопками главной (home.ejs href="#", §9 — функционал с ПДн-последствиями):

- [x] Регистрация игрока — публичная форма с нуля (/register нет): ввод -> БД -> МОДЕРАЦИЯ -> чекбокс согласия + запись в журнал. rate-limit на /register. МИНИМИЗАЦИЯ (ч.5 ст.5): обязательные/необязательные поля. СТРАНИЦА СТАТУСА заявки после подачи. EMAIL игроку: подана / одобрена / отклонена (SMTP Яндекс, с fallback). ДУБЛИКАТ (игрок уже введён секретарём): НЕ дубль/НЕ автослияние — совпадение ФИО помечается модератору «возможное совпадение» -> привязка к существующему или новый. Ручной ввод секретарём: офлайн-согласие + ОТМЕТКА в карточке (поле+дата) + ПРАВОВОЕ ОСНОВАНИЕ на публикацию ФИО (для заводимых админом вручную — зафиксировать основание, не только согласие).
  СДЕЛАНО 08.08.2026: /register (форма) + /register/status/:token (страница статуса по секретной ссылке) + /admin/registrations (модерация). Форма ТОЛЬКО POST, CSRF, honeypot, черновик в сессии при ошибке, явные состояния ошибок списком.
  ДВА РАЗДЕЛЬНЫХ чекбокса, ни один не предзаполнен (ч.8 ст.10.1); согласие на обработку обязательно, на публикацию — добровольно. Пишутся двумя записями журнала с одной LEGAL_VERSION, привязка consents.registration_id -> при одобрении доливается player_id.
  МИНИМИЗАЦИЯ: обязательны ФИО, город, пол, e-mail; возрастная группа необязательна; телефона и даты рождения НЕТ.
  RATE-LIMIT: publicFormLimiter, 5 заявок с адреса в час (REGISTER_MAX_PER_WINDOW), счётчик отдельный от админского.
  ДУБЛИКАТ: нормализация ФИО (регистр, ё/е, пробелы) -> «возможное совпадение» подсказкой модератору. Автослияния НЕТ. Одобрение либо заводит нового игрока, либо привязывает к существующему. Отдельно показывается РАСХОЖДЕНИЕ ПО ПУБЛИКАЦИИ (заявитель не разрешил, а тёзка публикуется) — решает модератор, чужой флаг молча не меняется.
  ПОЧТА: mail_outbox — письмо сперва в очередь, потом отправка. ТРАНСПОРТ — nodemailer (server/lib/smtp.mjs), реквизиты только из окружения: SMTP_HOST/SMTP_PORT/SMTP_SECURE/SMTP_USER/SMTP_PASS/MAIL_FROM, в .env.example заглушки. Пароль приложения в логи не печатается (только хост и ящик). Без реквизитов отправка выключена: заявки принимаются, очередь копится, в лог идёт предупреждение. Fallback «не молча»: неотправленное видно в /admin/registrations с причиной + кнопка повтора; периодический разбор очереди раз в MAIL_RETRY_MINUTES, после MAIL_FAIL_AFTER неудач письмо помечается failed и ждёт кнопки.
  RETENTION заявок: REGISTRATION_RETENTION_DAYS=365 для отклонённых и брошенных; одобренные живут с игроком (каскад). Планировщик очисток общий — lib/retention.mjs.
  Кнопки «Регистрация» и «Регистрация игрока» ОЖИВЛЕНЫ (шапка, главная, подвал). Приёмка: 12 новых проверок, всего 70/70.
  РУЧНОЙ ВВОД СЕКРЕТАРЁМ: публикация игрока, заведённого вручную, ТРЕБУЕТ правового основания — поля consents.basis + consents.document_date обязательны при включении публикации (галочка основанием не является). Видны в карточке игрока. Отзыв основания не требует.
  ВЛАДЕЛЬЦУ ПРИ ВЫКАТЕ: завести SMTP_USER/SMTP_PASS (пароль ПРИЛОЖЕНИЯ Яндекса) в .env на сервере — до этого письма не уходят, заявителей уведомлять вручную. nodemailer добавлен в зависимости: прогнать npm audit в выкат (как и записано в блоке B).
  Приёмка почты: письмо доходит до транспорта и помечается sent; сбой SMTP НЕ теряет заявку (письмо ждёт с причиной, после исчерпания попыток failed и видно в админке, кнопка повтора досылает); пароль не в логах и не в git.

- [x] Провести турнир — заявка ФОРМОЙ + документы (положение/сетка) загрузкой файлов (см. ЕДИНЫЕ ТРЕБОВАНИЯ). Результаты для рейтинга — структурно через готовый CRUD админки. + модерация.
  СДЕЛАНО 08.08.2026: /tournament-request (форма multipart) + /tournament-request/status/:token + /admin/tournament-requests (модерация) + /admin/files/:id (отдача документов за логином).
  ЕДИНЫЕ ТРЕБОВАНИЯ — ОДИН слой server/lib/uploads.mjs, галерея и /documents сядут на него: тип по magic bytes; исполняемое (PE/ELF/Mach-O/Java class/shebang) отбивается отдельным списком первым; голый ZIP не проходит как docx (проверяется имя первой записи [Content_Types].xml); лимит и разрешённые типы по ПРОФИЛЮ (tournament-doc / gallery / documents); имя на диске своё случайное — присланное в пути не участвует; хранение в site/storage (ВНЕ webroot, статикой не раздаётся); отдача только attachment + nosniff; РЕСАЙЗ через sharp до 1600px с ПОЛНОЙ ПЕРЕЗАПИСЬЮ — срезает EXIF (геолокация, модель камеры = ПДн).
  MULTIPART — busboy (server/lib/multipart.mjs), потоковый обрыв при превышении лимита. CSRF проверяется ВНУТРИ парсера: общий middleware для multipart тело ещё не разобрал, поэтому пропускает, а получить поля формы, не сверив токен, нечем.
  Заявка -> tournament_requests + файлы в tournament_request_files; в tournaments турнир попадает ТОЛЬКО согласованием. Результаты файлом НЕ принимаются — только структурно через CRUD (проверено тестом: у созданного турнира results = 0).
  Согласие организатора (ФИО/почта/телефон = ПДн) пишется в журнал как processing; на распространение не требуется — контакты организатора не публикуются.
  Rate-limit СВОЙ счётчик (ключ «t»), 3 подачи с адреса в час; проверено, что он не перекрывает регистрацию игроков. Черновик текстовых полей при ошибке сохраняется, файлы браузер не отдаёт повторно — сказано в форме прямым текстом. При любой ошибке уже записанные файлы убираются с диска (осиротевший файл = утечка).
  RETENTION: TOURNAMENT_REQUEST_RETENTION_DAYS=365 для отклонённых и брошенных — чистится ВМЕСТЕ С ФАЙЛАМИ на диске; согласованные живут.
  Кнопка «Провести турнир» на главной ОЖИВЛЕНА. Приёмка: 13 новых проверок (в т.ч. «загрузка идёт только через общий слой» вместо устаревшей «аплоада нет»), всего 83/83.
  ВЛАДЕЛЬЦУ ПРИ ВЫКАТЕ: busboy и sharp в зависимостях — npm audit. sharp тянет нативный бинарь: здесь встал готовый, на VPS под Node 22 обычно тоже; если попросит компилятор — это блок 0, сигнал владельцу, а не пляска на сервере. Каталог site/storage должен быть доступен на запись процессу pm2 и НЕ раздаваться nginx.
  НЕ ВХОДИТ: публичная отдача документов на карточке турнира — это пункт «Карточка турнира /tournaments/:id»; сейчас документы видны только модератору.

- [x] Личный кабинет игрока — вход, профиль, свой рейтинг/история. РЕДАКТИРУЕМО: ФИО, email, фото; НЕ: рейтинг. ПОЛИТИКА ПАРОЛЕЙ (мин. длина/сложность). УДАЛЕНИЕ (ст.21) = реальное обезличивание записи игрока в БД (не скрытие из UI); МАТЧИ удалённого игрока — ПСЕВДОНИМИЗАЦИЯ («Игрок удалён»), НЕ снос (матч влияет на рейтинг соперников). СБРОС ПАРОЛЯ по EMAIL. ОТЗЫВ сессий при смене пароля. Страница «нет доступа»/статус для незалогиненного.
  СДЕЛАНО 08.08.2026: /cabinet (профиль, свои результаты и матчи), /cabinet/login, /cabinet/forgot, /cabinet/reset/:token, /cabinet/password, /cabinet/publication, /cabinet/delete. Сессия игрока ОТДЕЛЬНО от админской (req.session.player против req.session.user). Свой rate-limit (ключ «c»).
  ДОСТУП: аккаунт (player_accounts) создаётся при ОДОБРЕНИИ заявки без пароля; игрок задаёт пароль сам по одноразовой ссылке из письма. Пароль за человека не придумывается и по почте не шлётся. В БД хранится ХЭШ токена, не сам токен.
  РЕДАКТИРУЕМО: ФИО, почта, фото (через общий слой загрузки — ресайз и снятие EXIF). НЕ редактируемо: рейтинг, очки, места (проверено тестом — полей рейтинга в форме нет). Город и группу меняет секретарь.
  ПОЛИТИКА ПАРОЛЕЙ: минимум 10 символов, буквы + цифра/знак, запрет очевидных слов, запрет собственных почты и имени. Сброс по почте: ответ ОДИНАКОВЫЙ для существующего и несуществующего адреса (форма не должна отвечать, зарегистрирован ли человек).
  ОТЗЫВ СЕССИЙ при смене пароля: своя жива, остальные умирают немедленно (не по истечении).
  СТРАНИЦА «НЕТ ДОСТУПА» — объясняет путь заявка -> одобрение -> письмо, а не сухой отказ.
  УДАЛЕНИЕ (ст.21) = ОБЕЗЛИЧИВАНИЕ В БД (server/lib/erasure.mjs), не DELETE и не скрытие в UI. Затирается: ФИО, город, группа, фото (файл с диска), аккаунт с почтой и хэшем, согласия, заявки, письма, ИМЯ В СНИМКАХ РЕЙТИНГА, личные значения в journal действий (событие/автор/время остаются), ФИО в ДАННЫХ СЕССИЙ (оседает во флеш-сообщениях). Затем VACUUM + wal_checkpoint(TRUNCATE): без них затёртое читается grep-ом из файла БД и особенно из .sqlite-wal.
  СОХРАНЯЕТСЯ: строка игрока как анонимная вершина, результаты и матчи — они влияют на рейтинг СОПЕРНИКОВ. На витрине «Игрок удалён» С СОХРАНЕНИЕМ МЕСТА И ОЧКОВ. Слой обезличивания на отрисовке теперь на ТРИ случая: нет согласия / обезличен по ст.21 / нет в БД.
  ПРОВЕРЕНО ТЕСТАМИ ОБЕ СТОРОНЫ: (1) ни ФИО, ни адреса нет НИ В ОДНОМ файле БД (основной + WAL); (2) места и очки ВСЕХ соперников совпадают до и после удаления, обезличенный стоит на своём месте.
  Приёмка: 9 новых проверок, всего 92/92. Кнопки «Личный кабинет» оживлены (главная, подвал).

Публичные разделы (контент):

- [x] Новости /news — публичный список + страница + админ-CRUD.
  СДЕЛАНО 08.08.2026: /news (список) + /news/:id + админ-CRUD /admin/news (роли super-admin, news-editor, content-manager).
  ЧЕРНОВИК И ПУБЛИКАЦИЯ разведены: is_published. Черновик не виден в списке И не отдаётся по прямой ссылке — /news/:id возвращает 404 (проверено тестом).
  Текст выводится ЭКРАНИРОВАННО с сохранением абзацев: HTML из админки в разметку не попадает. Блок новостей на главной — живой, три последние.

- [x] Список турниров /tournaments — публичная страница-список (сейчас только /tournaments/:id).
  СДЕЛАНО 08.08.2026: /tournaments — календарь с датой, категорией и числом участников, ссылка на карточку. Блок турниров на главной тоже живой (пять последних). Ссылка «Провести свой турнир» ведёт на форму заявки.

- [x] Карточка турнира /tournaments/:id — ПОЛЯ: сетка, результаты, участники; при наличии — документы/фото. Ссылки на профили игроков — НЕТ на старте (публичной карточки игрока нет).
  СДЕЛАНО 08.08.2026: участники с местами, сетка сыгранных матчей, документы.
  ССЫЛОК НА ПРОФИЛИ ИГРОКОВ НЕТ — публичной карточки игрока в проекте нет (проверено тестом).
  ИМЕНА ПРОХОДЯТ ТО ЖЕ ОБЕЗЛИЧИВАНИЕ, что и рейтинг: нет согласия -> «Скрыто по заявлению», удалён по ст.21 -> «Игрок удалён». Иначе протокол турнира стал бы обходным путём вокруг согласия (проверено тестом: отзыв согласия убирает ФИО из протокола).
  ДОКУМЕНТЫ отдаются через /files — из storage вне webroot, attachment + nosniff, статикой не раздаются. Публичным файл считается ТОЛЬКО если привязан к опубликованной сущности: документ заявки, ждущей модерации, по /files не отдаётся (проверено). При согласовании заявки её файлы переезжают на сам турнир (tournament_files) — заявку однажды вычистит срок хранения, а положение турнира останется.

- [x] Разделы-справочники — через CRUD (свои таблицы + админ-формы, как игроки; НЕ статика): Тренеры /coaches (ФИО, клуб, контакт), Корты /courts (адрес, покрытие, карта), Клубы /clubs, Судьи /referees (категории).
  СДЕЛАНО 08.08.2026: /coaches, /courts, /clubs, /referees + админка /admin/directories/:key. ОДНА реализация CRUD на четыре раздела (server/lib/directories.mjs): раздел описан ДАННЫМИ (таблица, поля, порядок), код общий — четыре копии разошлись бы так же, как разошлись бы четыре копии проверки файлов.
  ФИО ТРЕНЕРА И СУДЬИ — ПДн: их публикация это распространение (ст.10.1), поэтому у coaches и referees ОБЯЗАТЕЛЬНЫ основание публикации и дата документа (как у игроков, заводимых секретарём). Без основания запись не сохраняется; само основание — служебное поле и на витрину не выводится (проверено тестом). Корты и клубы — про объекты, не про людей: основания не требуют.

- [x] Разделы контентные: О федерации /federation (история, руководство, реквизиты, уставные док-ты), Галерея /gallery (загрузка по ЕДИНЫМ ТРЕБОВАНИЯМ), Документы /documents (управляемый из админки список: файл+заголовок+категория; загрузка по ЕДИНЫМ ТРЕБОВАНИЯМ), Контакты /contacts (реквизиты + телефон + карта).
  СДЕЛАНО 08.08.2026: /federation (описание, реквизиты, уставные документы), /gallery, /documents (по категориям), /contacts (реквизиты + телефон + карта).
  КАРТА — ССЫЛКОЙ, а не встроенным фреймом: сторонний iframe запрещён строгим CSP и притащил бы на страницу чужие счётчики. Ноль внешних запросов сохраняется.
  ГАЛЕРЕЯ и ДОКУМЕНТЫ зовут ОБЩИЙ слой загрузки (профили gallery / documents): magic bytes, лимит, вне webroot, attachment, ресайз с чисткой EXIF. Своих проверок в маршрутах нет — за этим следит проверка приёмки «загрузка идёт только через общий слой».
  РУКОВОДСТВО федерации не вписано в шаблон: это ФИО живых людей, они ведутся справочником с зафиксированным основанием публикации.

Общее по фронту:

- [x] Мобильный адаптив — ЕДИНЫМ ПРОХОДОМ после всех шаблонов (не копить долг). КРИТЕРИЙ: Chrome DevTools на 360px и 768px без горизонтального скролла.
  СДЕЛАНО 08.08.2026 единым проходом. КРИТЕРИЙ ВЫПОЛНЕН: 17 публичных страниц на 360 и 768 px без горизонтального скролла — проверяется автоматически в приёмке (Chromium), причём тест называет КОНКРЕТНОГО виновника переполнения, а не просто «шире окна».
  Поймано и исправлено: fieldset с полем выбора файла распирал страницу на 360px (у fieldset min-inline-size по умолчанию min-content) — /tournament-request уезжал на 89px.

- [x] Доступность (a11y) — alt у изображений, достаточный контраст, видимый фокус, лейблы у полей форм, работа с клавиатуры. Тем же проходом, что адаптив.
  СДЕЛАНО 08.08.2026 тем же проходом. Проверяется автоматически по тем же 17 страницам: lang="ru", ровно один h1, наличие <main> и ссылки «к содержимому», МЕТКА У КАЖДОГО поля формы (label/aria-label/aria-labelledby), alt у изображений, ссылки с различимым текстом, ВИДИМЫЙ ФОКУС и работа с клавиатуры (первый Tab попадает на skip-link, контур фокуса не снят).
  Таблицы получили caption для скринридера (.visually-hidden — виден программе чтения, не занимает места; display:none тут не годится).

## B. Инфраструктура, безопасность, эксплуатация (Claude в чате + владелец)

- [ ] logrotate для nginx/pm2 — АКТУАЛЬНО СЕЙЧАС: логи с IP копятся, сайт в сети = минимизация ПДн.

- [x] ~~ПРОВЕРКА RESTORE~~ — 04.09.2026 на бою: последний архив распакован во временный каталог, копия БД читается (игроков 0, загрузок 0 — база пуста), регдоки /documents лежат в git (site/public/docs), бэкапу не нужны. Офсайт — см. выше.

- [ ] uptime-мониторинг сайта.

- [ ] Этап 3 — домен ftso67.ru: A-запись в DNS Timeweb на 194.87.187.42 (CUTOVER — DNS только по слову владельца) + SSL Let's Encrypt (certbot) + HTTP->HTTPS редирект.

- [ ] Redeploy — после каждого смерджа в main прогнать 02-deploy-ftso.sh; npm audit прогонять в КАЖДЫЙ выкат (не только разово в блоке 0).

## C. Готовность и юридическое (владелец / федерация)

- [ ] Тест движка рейтинга на РЕАЛЬНЫХ данных ДО go-live — датасет готовит ФЕДЕРАЦИЯ (первая порция); объём достаточен при игроках с >6 турнирами и общими соперниками (окно 52 нед / отсев 6 лучших / тай-брейк). ПРОЙДЕНО = движок совпал с ручным расчётом.

- [ ] Прогнать WebGuardReport на ftso67.ru перед go-live. КРИТЕРИЙ: блокер = находки «требует по закону» (штрафные статьи 152-ФЗ) — чинить до go-live; «рекомендации» — не блокер. Аудит владельца, правки кода делает код вэб / Claude, решение о допустимости рисков — владельца.

- [ ] Уведомление об УТЕЧКЕ ПДн (регламент федерации, не код) — при инциденте: субъектам 24ч, в РКН 72ч (ч.3.1 ст.21).

- [ ] Уведомление оператора ПДн в Роскомнадзор — owner федерация, до начала обработки ПДн. КЛЮЧ ОТ ОТКРЫТИЯ: код готов, дверь открывает федерация.

- [ ] Приказ о лице, ответственном за организацию обработки ПДн (ст. 22.1) — контакты уже названы в §13 Политики.

- [ ] Договор с почтовым провайдером как ПОРУЧЕНИЕ обработки (ст. 6 ч. 3) — клаузула в §6.1 Политики уже опубликована.

- [x] ~~ЖИВОЙ SMTP~~ — закрыто решением владельца 04.09.2026 (см. выше). Очередь писем работает, транспорт не настраиваем.

- [ ] Подтвердить формулировку срока хранения данных представителя в бумажной Политике (в коде 3 года от снятия гейта, GUARDIAN_RETENTION_DAYS=1095).

- [ ] Регламент хранения БУМАЖНЫХ согласий — задача ФЕДЕРАЦИИ, кода не требует (где скан/оригинал, кто, срок).

- [ ] Наполнение реальными данными через админку.

- [ ] Самостоятельная выгрузка своих данных по ст. 14 (self-service). НЕ блокер: право закрыто обращением к оператору, порядок и сроки — §12 Политики.

## ПРОВЕРЕНО — уже в ядре, заново НЕ чинить (замер по коду 121022e)

- security-заголовки БОГАТЫЕ: CSP вручную (default-src self; script-src self+nonce; frame-ancestors none; object-src none; base-uri self; form-action self; img-src self+data:), X-Frame-Options DENY, Referrer-Policy strict-origin-when-cross-origin, nosniff, HSTS за HTTPS в проде.

- СЕССИЯ: rolling + maxAge 12ч = истечение по бездействию (idle timeout) уже есть.

- action_log ПОЛНЫЙ (вход/выход, CRUD всех сущностей, recompute, пароли) — дыр аудита нет.

- rate-limit на /login (login-attempts по «логин+IP») и на запись (writeLimiter). 403 для неверной роли — есть.

- SESSION_SECRET в .env вне git; ротация — ТОЛЬКО при компрометации.

- трансграничной передачи ПДн НЕТ by design: шрифты самохост, почта Яндекс (РФ), сервер MSK (РФ). Куки только сессионные, аналитики нет.

- СРОК ХРАНЕНИЯ профилей — НЕ автоснос: профиль живёт, пока игрок в системе; удаление по заявлению (право на забвение).

## Порядок к запуску (с гейтами — спор про SSL снят: разработка форм НЕ ждёт SSL, боевой приём ПДн ждёт)

1. БЛОК 0 (npm audit -> пароль admin -> права файлов -> 1b)

2. logrotate (логи с IP копятся уже сейчас)

3. Юридический контент (/privacy, /consent, журнал согласий)

4. РАЗРАБОТКА форм на HTTP с ТЕСТОВЫМИ данными — регистрация/турнир/ЛК пишутся и тестируются БЕЗ реальных ПДн (тестовые данные ≠ ПДн, HTTPS для этого не нужен)

5. Достройка разделов (справочники CRUD, контентные)

6. Адаптив + a11y единым проходом

7. Бэкап БД + проверка restore

8. Тест движка на реальных данных (датасет федерации)

9. Свой WebGuardReport-аудит и починка блокеров

10. Домен ftso67.ru + SSL (cutover, DNS по слову Олега) + HTTP->HTTPS

11. ОТКРЫТИЕ публичного приёма реальных ПДн — ТОЛЬКО после SSL (до этого формы работают на тестовых)

12. go-live
