# BACKLOG — сайт ФТСО (репозиторий oktigra/ftso)

Живой список работ по сайту федерации тенниса Смоленской области.

Код вэб (Claude Code) читает его в начале каждой сессии и обновляет статусы в конце.

Это лёгкая замена «домика»: непрерывность держат этот файл + память Claude в чате + хендофф-док владельца.

## Состояние на 07.08.2026

Сайт РАЗВЁРНУТ на VPS ftso67 (http://194.87.187.42), но это КАРКАС: ядро работает, публичная витрина почти пустая.

ГОТОВО (ядро — не трогать без причины):

- движок рейтинга (rating/), авторизация, сессии, CSRF, security-заголовки, экранирование;

- главная по дизайну; раздел рейтинга (/rating: фильтры, экспорт CSV);

- админка за логином: CRUD игроков, турниров, результатов, пользователей, смена пароля, дашборд;

- инфра: Node 22 + pm2 (процесс ftso, 127.0.0.1:3000), nginx :80 -> 3000, деплой из main, автозапуск.

Правило раскладки: приложение в site/, движок в rating/ рядом — относительные импорты НЕ ломать. npm-команды из site/. БД пустая (боевой без сида) — наполняется через админку.

## A. Достройка публичной витрины (код вэб) — главный фронт работ

- [ ] Политика /privacy — заменить пустой шаблон (views/legal.ejs) реальным текстом политики обработки ПДн по 152-ФЗ с реквизитами оператора (РФСОО «ФТСО»). Готово, когда: страница содержит полную политику, не рыбу.

- [ ] Согласие /consent — реальный текст согласия на обработку ПДн (сейчас тот же пустой шаблон). Готово: полный текст.

- [ ] Cookie-баннер — рабочее согласие на cookie: баннер + сохранение выбора + необязательные скрипты не грузятся до согласия. Готово: баннер появляется, выбор запоминается между визитами.

- [ ] 7 портальных кнопок на главной (сейчас href="#") — посмотреть их подписи в views/home.ejs, определить назначение каждого раздела, построить страницы. Сначала: согласовать список разделов; потом реализация.

- [ ] Новости /news — раздел новостей (публичный список + страница новости) + управление из админки. На /news есть ссылка, страницы нет. Готово: публичный список + админ-CRUD.

- [ ] Список турниров /tournaments — публичная страница-список (ссылка есть, страницы нет; сейчас существует только /tournaments/:id). Готово: список ведёт на карточки.

- [ ] Карточка турнира /tournaments/:id — дописать тонкий шаблон (views/tournament.ejs): сетка/результаты/участники. Готово: полноценная карточка.

## B. Инфраструктура и деплой (Claude в чате + владелец)

- [ ] Этап 1b — SSH-ключ на root ftso67 + отключить вход по паролю + хардендинг fail2ban. ТОЛЬКО после проверки входа по ключу.

- [ ] Этап 3 — домен ftso67.ru: A-запись в DNS Timeweb на 194.87.187.42 (это CUTOVER со старого сайта — DNS трогаем только по прямому слову владельца) + SSL Let's Encrypt (certbot).

- [ ] Redeploy — после каждой пачки правок сайта прогнать 02-deploy-ftso.sh для обновления боевого.

## C. Compliance и юридическое (владелец / федерация)

- [ ] Сменить пароль супер-админа (логин admin) при первом входе — временный засветился в чате.

- [ ] Прогнать сам WebGuardReport на ftso67.ru перед go-live и починить подсвеченное. Свой аудит на своём клиенте — обязательный compliance-гейт.

- [ ] Уведомление оператора ПДн в Роскомнадзор — юр-действие федерации, не код (сайт с именами игроков под это попадает).

- [ ] Наполнение реальными данными — игроки/турниры/результаты через админку.

## Порядок к запуску

Достроить витрину (A) -> наполнить данными -> прогнать свой аудит и починить (C) -> домен + SSL (B, этап 3) -> сменить пароль -> go-live.
