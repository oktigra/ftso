# ВХОД В ЧАТ ПО ФТСО — нулевой шаг всегда здесь

Память проекта ФТСО живёт В ЭТОМ репозитории, не в домике WGR (`claude-house` про
ФТСО не знает — проверено 03.09.2026 грепом по 97 сессиям). Чат по памяти не
работает: состояние снимается заново каждую сессию.

## Входной пакет (Олег прикладывает в первом сообщении)

1. ТЗ или письмо задачи.
2. `gh_token_write.txt` — токен с записью в `oktigra/ftso` (проба: POST `git/refs`
   с нулевым sha → `422` = запись есть).
3. Всё остальное чат берёт сам: клон, `BACKLOG.md`, `COMPLIANCE.md`, этот файл.

## Нулевой шаг — три команды, без них ни строки вывода о состоянии

```
git clone https://x-access-token:$GH_TOKEN@github.com/oktigra/ftso.git && cd ftso
cd site && npm ci --silent && cp .env.example .env   # SESSION_SECRET и пароли — любые строки
node OPEN.mjs            # клон, legal.mjs, бой снаружи; --accept — приёмка (~2,5 мин)
```

Дальше — блок «ВХОД СЛЕДУЮЩЕЙ СЕССИИ» ниже (последние два), затем `BACKLOG.md`.

## Правила этого репозитория, которые не выводятся из общих

- Доставка: ветка + PR, патчи запрещены. Мерж в `main` → автовыкат на бой за ≤2 мин
  (`deploy/deploy-watch.sh` → `/root/deploy-A.sh`: `npm install`, `npm run migrate`,
  `pm2 restart`, проверка 200 по `/`, авто-откат). Новые страницы `/` не проверяет —
  после выката замерять их снаружи самому.
- Репозиторий ПУБЛИЧНЫЙ: в снимках и коде — ни токенов, ни паролей, ни почт, ни ПДн.
- Модель публикации — РТТ (ТЗ ред. 6, 02.09.2026): результаты по факту участия,
  согласие и 18 лет на витрину не влияют. Старые тексты про «Скрыто по заявлению»
  и «снятие в 18» в `COMPLIANCE.md` — история.
- Выход из хода: приёмка сошлась числами либо назван красный, который чат сам не
  закроет. Вопросы «продолжать?» и «скажи, если мимо» — нельзя.
- Закрытие сессии — ТРАФАРЕТ РИТУАЛА, один на все проекты, лежит в
  `oktigra/orchestrator-sandbox` (`tools/ritual.mjs`, описание проекта —
  `tools/ritual/ftso.json`; форма пачки — в шапке скрипта). Пачка кладётся в
  `ritual/` этого клона, затем:
  ```
  git clone https://x-access-token:$GH_TOKEN@github.com/oktigra/orchestrator-sandbox.git /tmp/sandbox
  GH_TOKEN=$GH_TOKEN node /tmp/sandbox/tools/ritual.mjs ftso <N> "<сообщение>" --clone "$PWD"
  ```
  Кнопки владельца нет: скрипт сам открывает и сливает PR; замок — в PR ритуала не
  может быть ничего, кроме `sessions/`, `ritual/` и этого файла.

<!-- RITUAL:BLOCKS — ниже скрипт ритуала вставляет блок входа; держатся два последних -->

## ⚡ ВХОД СЛЕДУЮЩЕЙ СЕССИИ — START16_V1

Состояние на момент записи (12.09.2026): `main` `035c081`, бой `site.css?v=fdf39ac7`, приёмка 238/238. Витрины: рейтинг 59 / 19 / 10 (одиночный / парный / микст), турниров 3 (все прошедшие), новостей 4, игроков 104, галерея 12. Задание START15 закрыто: задачи 1–5 и 7 (код) сделаны, 6 — не начата сознательно.

### Первым делом
1. Нулевой шаг: клон → `npm ci` → `cp .env.example .env` и СРАЗУ случайные `SESSION_SECRET`, `SUPER_ADMIN_PASSWORD`, `TOURNAMENT_ADMIN_PASSWORD` → `node OPEN.mjs` (теперь печатает пластины `/rating`). Стоп-условие приёмки — код выхода ≠ 0 либо `grep -c ПРОВАЛЕНО` > 0 (кириллица!).
2. Почта оркестратор ↔ браузерный — файл `ftso-checklist` в памяти проекта ФТСО (описание «ПОЧТА ФТСО…»); прочитать, ответить, исполненное стереть (почта — очередь, не архив).

### Хвосты с прошлой сессии
- **Мониторинг (задача 7):** ветка с `.github/workflows/uptime.yml` (раз в 15 минут: главная 200 + имя федерации, `/rating` 200, сертификату > 10 дней) не допушена — у `gh_token_write` нет права **Workflows**. Когда владелец приложит токен с «Workflows: Read and write» — воссоздать файл из BACKLOG-описания (в клоне его нет), push, PR, мерж, затем проверить вкладку Actions: первый прогон зелёный.
- **Реестр тренеров (задача 6):** ждёт слова владельца; три варианта в BACKLOG. Без его слова 16 тренеров не публиковать.
- **Даты рождения игроков** — все возрастные срезы пусты; данные за федерацией.
- Словарь систем проведения (`TOURNAMENT_FORMAT_RU` в `site/server/lib/validate.mjs`) выбран чатом — владелец может переименовать.

### Что помнить при коде
- Микст — третий список (`standings.mixed`), `tableFor(standings, discipline)` в `rating-service.mjs`; парная сетка — `partner_id` в `bracket_slots`, `resolveEntrant()` в `brackets.mjs`.
- Статус приёма — `entryStatus()` / `entryDeadline()` в `content.mjs`; сетка месяца — `calendarGrid()` там же, cookie `ftso.calendar`.
- Новые элементы над ящиками `/tournaments` проверять наведением при 1440×900 (полоса cookie внизу).

## ⚡ ВХОД СЛЕДУЮЩЕЙ СЕССИИ — START15_V1

### ЗАДАНИЕ СЛЕДУЮЩЕЙ СЕССИИ (выдано владельцем 09–10.09.2026, порядок мой)

Состояние на момент записи: `main` `fc4856f`, бой `site.css?v=e2a965a2`, приёмка 232/232, открытых заявок нет. Турниров на витрине 3, рейтинг 54 строки (после слияния Коротковых пересчёта ещё не было), галерея 12 снимков, новостей опубликовано 0, реестр тренеров пуст.

**1. Места в возрастных срезах — привести к правилам РТТ.** Сейчас в срезе по полу места пересчитываются с первого, а в общем списке они общие; поведение разъезжается. Решение владельца: «посмотри как в РТТ и сделай так же». ПЕРВЫМ ДЕЛОМ проверить у источника (tennis-russia.ru, rttstat.ru), как нумеруются места внутри возрастной группы и внутри пола, и только потом трогать код. Не решать по памяти.

**2. Поле «формат турнира».** Владелец: «рассмотри все варианты как у других клубов». Разведка по календарям других федераций и клубов: какие форматы вообще встречаются (выходного дня, недельный, круговой, с групповым этапом, микст-серия и т.д.), затем поле в админке + фильтр на календаре. Значения согласовать с владельцем до реализации.

**3. Статус приёма заявок на турнир.** Сейчас `entry_deadline` — просто дата в карточке, сайт не понимает, открыт приём или закрыт. Владелец: «делай правило и реализуй». Правило предложить самому (например: до срока «приём открыт», в последние N дней «поздняя заявка», после — «закрыт»), показать владельцу одной строкой и сделать. Статус выводить на карточке турнира и в календаре, заложить в фильтр «статус».

**4. Календарный вид турниров с ПЕРЕКЛЮЧЕНИЕМ.** Владелец: «нужен, но с переключением видов с того как сейчас на то как в РТТ». То есть ящики остаются, рядом появляется вид «месячная сетка» как у РТТ, выбор запоминается. Смотреть, как это устроено у РТТ, до вёрстки.

**5. Парные сетки с посевом.** Самая крупная задача: пара как участник сетки, посев пары, партнёры, отражение в результатах и в парном рейтинге. Начинать с замера существующей модели `tournament_brackets` / `bracket_slots` и `matches` (там уже есть `winner_partner_id` / `loser_partner_id`).

**6. Реестр тренеров — «публикуй то, что можно».** 16 тренеров выписаны со старого сайта. Публиковать без согласия можно только то, что не является персональными данными в смысле 152-ФЗ, либо то, на что есть основание. Предложить владельцу конкретный минимум (например, только клуб/место работы без ФИО — или ФИО тех, кто уже публичен как должностное лицо федерации), получить его слово, потом публиковать. НЕ публиковать ФИО+контакты скопом. Анкеты `/coaches/apply` и `/referees/apply` с постатейным согласием уже работают — это основной путь наполнения.

**7. Мониторинг доступности — предложить бесплатные варианты** (UptimeRobot, Better Stack, Hetrix, статус-чек через сам VPS), сравнить по частоте проверок, каналам уведомлений и ограничениям бесплатного тарифа; выбор за владельцем.

**ХВОСТЫ, НЕ ТРЕБУЮЩИЕ КОДА:**
- Четыре тестовых игрока «Тестов Один…Четыре» (`/player/3..6`) видны в поиске — удалить в `/admin/players` (окно подтверждения, поэтому руками).
- Неявка в клетке Лобанова — Третьякова (группа ж45+, турнир 6): вводить «неявка 1 и 2», затем пересчёт рейтинга в админке. Проверено браузерным агентом: результата матча в канале MAX нет (28 постов, 32 кадра, клетка пуста), решение владельца — неявка обоим.
- Публикация четырёх черновиков новостей. Текст №3 переписать: он зовёт слать ПДн на почту, а анкеты с постатейным согласием уже работают — дать ссылки на них.
- Логотипы партнёров (6 файлов) и цветной логотип «Алпины» — у владельца.

- РЕЙТИНГ ПРОВЕРЕН НЕЗАВИСИМЫМ ПЕРЕСЧЁТОМ (09.09, вечер): скачаны боевые страницы трёх турниров и `rating.csv`, очки посчитаны СВОИМ кодом по описанию из `rating/README.md` (движок проекта не вызывался) — **55 из 55 совпало, расхождений 0**. Нумерация мест верна: 1, 2×2, 4×5, 9, 10×8, 18×3, 21×7, 28×14, 42×10, 52×4 — сумма 55, пропуски рангов правильные. ГРАБЛЯ ПЕРЕСЧЁТА: на карточке турнира ТРИ таблицы (итоговые места, групповой этап, матчи) и в каждой есть `td.rank` — брать только секцию «Участники и места», иначе игрок получит по два-три места за турнир. И считать по разряду: в итоговой таблице есть колонка «Разряд».
- РАЗРЯДЫ КАК В РТТ (PR #205, проверено у источника tennis-russia.ru): классификация РТТ составляется РАЗДЕЛЬНО для одиночного и парного, СВОДНОЙ У НИХ НЕТ — не делать. У нас расчёт был такой изначально; витрина приведена: две равноправные вкладки над таблицей, фильтры переживают переключение, форма несёт разряд скрытым полем. На бою: одиночный 55, парный 19.
- ФИЛЬТР КАТЕГОРИЙ МНОЖЕСТВЕННЫЙ (PR #204): флажки, `?category=B&category=C`, одиночная категория в старых ссылках работает.
- ДЕФЕКТ, КОТОРЫЙ ЖИЛ ПОЛДНЯ И НАЙДЕН ТОЛЬКО ПОВЕДЕНИЕМ: пустая верхняя зона ящика (124px под выезд папок) НАКРЫВАЛА кнопку «Показать» на календаре — `elementFromPoint` в центре кнопки возвращал `li.tdrawer`, фильтровать было нельзя. Последствие подтяжки сетки под фильтры. Лечение: `pointer-events:none` на сетке и `auto` на корпусе ящика; в приёмке сторож на `elementFromPoint` для кнопки, списка города и флажка. ВЫВОД: после любой подтяжки/наложения проверять не вид, а достижимость управляющих элементов.
- ПРИЁМКА 231/231.

- ФОТО ИЗ MAX И ЗАГРУЗКА (вечер 09.09, второе дополнение). В галерее на бою **6 снимков** из 12 пригодных за 07.09 (ещё 3 позиции того дня — скриншоты сеток, в галерею не идут). Все шесть привязаны к турниру ветеранов и видны на `/gallery` и `/tournaments/6`. Шесть недостающих кадров — за браузерным агентом: MAX отдаёт файл только после отрисовки кадра в просмотрщике, и Chrome, судя по всему, глушит серию автоскачиваний (лечится в настройках сайта `web.max.ru` → автоматическое скачивание → разрешить). Из чата ни канал, ни диск Олега не видны.
- ЗАГРУЗКА ПАЧКОЙ (PR #200): форма `/admin/library` принимает до 10 кадров разом, подпись одна на пачку с автонумерацией, сбой на любом кадре откатывает всю пачку с файлами; при ОДИНОЧНОЙ загрузке номер не приписывается — так задумано.
- ПОТОЛОК ТЕЛА ЗАПРОСА (PR #201, #202 + команда на VPS 09.09): nginx резал на 1 МБ (замер: 1048576 проходило, 1100000 → 413 ДО приложения). `deploy/set-upload-limit.sh` поднял до 64m и вписал `UPLOAD_MAX_MB=60` в `/var/www/ftso/site/.env`, pm2 перезапущен. ПРОВЕРЕНО СНАРУЖИ ПОСЛЕ: 1.1 МБ, 5 МБ и 20 МБ дают 302. Форма печатает потолок в подсказке, `app.js` считает сумму выбранных файлов и не даёт отправить перевес — 413 рисует прокси, объяснить его постфактум нельзя.
- УРОК: расширил форму до десяти кадров, не проверив путь до сервера — обещание не проходило через nginx, нашлось боем. Прежде чем обещать объём в интерфейсе, мерить весь путь запроса, а не только своё приложение.
- НА ORIGIN 149 ВЕТОК, почти все слитые и старые (мои сегодняшние убраны). Уборка не делалась — чужие ветки без слова владельца не трогаю.

- БОЙ ОБНОВЛЁН И ПРОВЕРЕН 09.09.2026 (это дополнение к ритуалу 15, записано ПОСЛЕ мёржа — первая редакция блока говорила «заявки открыты», она устарела через час). `main` = `dc06c2c`, бой отдаёт `site.css?v=e0efe396`, локальный md5 сошёлся. Проверено НАВЕДЕНИЕМ на живом сайте, не по объявлению в CSS: календарь — 3 ящика, корпус `matrix(1,` → `matrix3d(`, третья папка выезжает на 120 px; рейтинг — 55 пластин, подъём 10 px, справка 0 → 96 px; корты — 36 карточек, заголовок в акцент, водяной знак 0 → 0.07; галерея и тренеры — приглашение с кнопкой; документы — 7 плиток; таблиц на витрине ноль.
- Приёмка `node acceptance.mjs` из `site/` → **227/227** (было 220/220). Выросла на семь: четыре проверки полноты полей после смены витрин и три БРАУЗЕРНЫЕ проверки поведением, плюс сторож пустых состояний.
- СДЕЛАНО В СЕССИИ 15 (PR #192–#195): ящики турниров на `/tournaments`; пластины рейтинга на `/rating`; карточки в справочниках (один шаблон `views/directory.ejs` = четыре страницы); пустые состояния как приглашения в справочниках, галерее и документах; документы плитками. Плоских разделов на витрине не осталось.
- ГРАБЛЯ, КОТОРУЮ ВАЖНО ЗНАТЬ: приёмка местами парсила HTML ТАБЛИЦ (`<td class="player">`, `<td class="t-name">`, `<td class="rank">`, `td.player`, счётчик строк справочника ловил пробелы внутри ячейки). После смены витрин все переписаны. На профиле игрока таблица ОСТАЛАСЬ — там парсер прежний, не трогать заодно.
- ЛИПКИЕ ЗАГОЛОВКИ ТАБЛИЦ — задача усохла и переформулирована: после перехода витрин на карточки таблицы на публичной части остались только на карточке турнира и в профиле игрока. Простым `position:sticky` внутри `.table-scroll` не возвращать (липнет к обёртке, замер на бою был top:-503); рабочий путь — обёртка с `max-height` и своей вертикальной прокруткой. Сторож в приёмке смотрит теперь карточку турнира.
- УРОК ПРО ЗАМЕРЫ (стоил двух ложных выводов): наводить мышь ЛОКАТОРОМ, он сам доводит элемент до вида; `scrollTo` перед `hover` сдвигает страницу, курсор попадает на соседа и эффект «не срабатывает». И сетки на `minmax(320–340px)` распирают 360 px — писать `minmax(min(100%, N), 1fr)`.
- ДАННЫЕ НА БОЮ: три турнира опубликованы (#4 чемпионат 13–19.07, #5 первенство до 13/до 17 24–29.08, #6 ветераны 2–7.09), 108 игроков, рейтинг — 55 в таблице, снимок от 08.09. Сверка рейтинга пройдена: 55 из 55, расхождений 0.
- РЕШЕНО ВЛАДЕЛЬЦЕМ, НЕ ПЕРЕСПРАШИВАТЬ: телефон на сайте — тот, что стоит в `server/lib/legal.mjs`; номер из договорного ТЗ и реестра ФТР устарел, на сайт не ставить.
- ОСТАТОК ПО РАЗВЕДКЕ РТТ (08.09.2026, источники: tennis-russia.ru, rustennistur.com, **rttstat.ru** — повторно разведку не гонять): (1) статус приёма заявок на турнир — «подача заявок / поздняя заявка / закрыт»; (2) календарный вид турниров вместо списка; (3) множественный выбор категорий в фильтре; (4) поле «формат турнира» — выходного дня / недельный; (5) сетки парных разрядов с посевом. НЕ делать: избранных игроков с уведомлениями и сохранённые фильтры — нужен кабинет с подписками, для области на 55 игроков избыточно.
- СУДЬИ, фактура: категории лестницей ЮС → 3К → 2К → 1К → ВК, следующая только при наличии предыдущей; категорию подтверждают раз в несколько лет — потому в карточке есть `category_date`. Значки ITF пишутся словами. Официальный учёт — карточка учёта в региональной коллегии судей, сайт даёт только витрину.
- ЖДЁТ ВЛАДЕЛЬЦА: логотипы партнёров (6 файлов готовы, «Алпина» белая — нужен цветной от клуба), фото из MAX (три поста 07.09), даты рождения игроков и имена 20 ветеранов по документам федерации, объединение записей Коротковых #2 и #21 в сторону #2, публикация 4 черновиков новостей, снятие плашки «режим разработки» кнопкой на Сводке.
- ЖДЁТ ФЕДЕРАЦИИ: матч Третьякова — Лобанова у женщин 45+ (доиграть или засчитать неявку — иначе круг открыт и места не пишутся); основания публикации и даты документов на 16 тренеров ЛИБО их анкеты через сайт; живо ли Сафоново (МЖК) и корты СГУС.
- ОТКРЫТЫЙ ВОПРОС МЕТОДИКИ: в срезах по полу места общие, в срезе по разряду — пересчитываются внутри выборки. Привести к одному поведению по решению федерации.
- СРОК: домен ftso67.ru оплачен до **16.11.2026** — продлить у RU-CENTER.
- Правила: палитра не меняется; `git status --porcelain` пуст → ветка; мерж только на зелёной со стоп-условием `grep PROVALENO`; токен `/mnt/user-data/uploads/gh_token_write.txt`.
- Закрытие: пачка в `ritual/`, затем `node /tmp/sandbox/tools/ritual.mjs ftso 16 "…" --clone "$PWD"`.

