# ВХОД В ЧАТ ПО ФТСО — нулевой шаг всегда здесь

Память проекта ФТСО живёт В ЭТОМ репозитории, не в домике WGR (`claude-house` про
ФТСО не знает — проверено 03.09.2026 грепом по 97 сессиям). Чат по памяти не
работает: состояние снимается заново каждую сессию.

## Входной пакет (Олег прикладывает в первом сообщении)

1. ТЗ или письмо задачи.
2. `gh_token_write.txt` — токен с записью в `oktigra/ftso` (проба: POST `git/refs`
   с нулевым sha → `422` = запись есть).
3. Всё остальное чат берёт сам: клон, `BACKLOG.md`, `COMPLIANCE.md`, этот файл.

## Нулевой шаг — три команды, без них ни строки вывода о состоянии

```
git clone https://x-access-token:$GH_TOKEN@github.com/oktigra/ftso.git && cd ftso
cd site && npm ci --silent && cp .env.example .env   # SESSION_SECRET и пароли — любые строки
node OPEN.mjs            # клон, legal.mjs, бой снаружи; --accept — приёмка (~2,5 мин)
```

Дальше — блок «ВХОД СЛЕДУЮЩЕЙ СЕССИИ» ниже (последние два), затем `BACKLOG.md`.

## Правила этого репозитория, которые не выводятся из общих

- Доставка: ветка + PR, патчи запрещены. Мерж в `main` → автовыкат на бой за ≤2 мин
  (`deploy/deploy-watch.sh` → `/root/deploy-A.sh`: `npm install`, `npm run migrate`,
  `pm2 restart`, проверка 200 по `/`, авто-откат). Новые страницы `/` не проверяет —
  после выката замерять их снаружи самому.
- Репозиторий ПУБЛИЧНЫЙ: в снимках и коде — ни токенов, ни паролей, ни почт, ни ПДн.
- Модель публикации — РТТ (ТЗ ред. 6, 02.09.2026): результаты по факту участия,
  согласие и 18 лет на витрину не влияют. Старые тексты про «Скрыто по заявлению»
  и «снятие в 18» в `COMPLIANCE.md` — история.
- Выход из хода: приёмка сошлась числами либо назван красный, который чат сам не
  закроет. Вопросы «продолжать?» и «скажи, если мимо» — нельзя.
- Закрытие сессии — ТРАФАРЕТ РИТУАЛА, один на все проекты, лежит в
  `oktigra/orchestrator-sandbox` (`tools/ritual.mjs`, описание проекта —
  `tools/ritual/ftso.json`; форма пачки — в шапке скрипта). Пачка кладётся в
  `ritual/` этого клона, затем:
  ```
  git clone https://x-access-token:$GH_TOKEN@github.com/oktigra/orchestrator-sandbox.git /tmp/sandbox
  GH_TOKEN=$GH_TOKEN node /tmp/sandbox/tools/ritual.mjs ftso <N> "<сообщение>" --clone "$PWD"
  ```
  Кнопки владельца нет: скрипт сам открывает и сливает PR; замок — в PR ритуала не
  может быть ничего, кроме `sessions/`, `ritual/` и этого файла.

<!-- RITUAL:BLOCKS — ниже скрипт ритуала вставляет блок входа; держатся два последних -->

## ⚡ ВХОД СЛЕДУЮЩЕЙ СЕССИИ — START14_V1

- Бой на `6485ecf`+ (main): приёмка `node acceptance.mjs` из `site/` → **220/220**. Перед словом «на бою» сверять `site.css?v=` боя с локальным md5 (js+css).
- ДАННЫЕ НА БОЮ: три турнира опубликованы (#4 чемпионат 13–19.07, #5 первенство до 13/до 17 24–29.08, #6 ветераны 2–7.09), 108 игроков, рейтинг — 55 в таблице, снимок от 08.09. Тексты протоколов — `protokoly/` в outputs чата (ветераны — редакция 2).
- СВЕРКА РЕЙТИНГА ПРОЙДЕНА: независимый пересчёт по методике против боевой таблицы — 55 из 55, расхождений 0. Пункт «тест движка на реальных данных до go-live» закрыт.
- РЕШЕНО ВЛАДЕЛЬЦЕМ, НЕ ПЕРЕСПРАШИВАТЬ: телефон на сайте — **8-995-104-32-62**. Номер +7 904 360-90-90 из договорного ТЗ и реестра ФТР устарел.
- Сделано в сессии 13 (PR #167–#189): оформление по ТЗ дизайн-чата (три темы и меню тем, партнёры вариант B, семь эффектов, ящик турнира, пластины рейтинга); стартовые партнёры и справочники по прежнему сайту (корты 10 → 35, клубы 3 → 7); инструкция Word `tools/build-guide.mjs` + кнопка «Скачать в Word» на /admin/guide; бланки согласия `tools/build-consent-forms.mjs`; **анкеты тренеров и судей с согласием по ст. 10.1** (/coaches/apply, /referees/apply, модерация в админке, основание «согласие через сайт от <дата>» проставляется само); поиск по РНИ; плашка «режим разработки» липкая и на всех страницах, включая админку. Липкие заголовки таблиц пробовались и СНЯТЫ в тот же день — см. ниже, почему.
- ШЕСТЬ ДЕФЕКТОВ, найденных на живых данных и исправленных: микст писался как `double` и затирал парное место (введена дисциплина `mixed` + миграция); незакрытый `</form>` на Сводке ломал кнопку пересчёта; микст подписывался «одиночный» в таблице мест; пол в миксте схлопывался в мужской; дубль игрока «Фамилия Имя» при заведённом «Фамилия Имя Отчество»; обязательное имя запирало карточки из импорта (20 записей ветеранов).
- **ПЕРВЫМ ДЕЛОМ — ЭФФЕКТЫ ТОЛЬКО НА ГЛАВНОЙ, ЭТО НЕДОДЕЛКА, А НЕ РЕШЕНИЕ.** Демо от дизайн-чата было одностраничным (`preview/index.html` — только главная), и оформление перенесено ровно туда. Замер 08.09: на `/tournaments` и `/rating` ноль `tdrawer__box`, ноль `rplate`, ноль `fx-card` — там прежние список и таблица. Перенести: (1) ящики турниров на `/tournaments` — карточек там десятки, нужна своя сетка и порядок, ящики уже готовы, риска меньше; (2) пластины рейтинга на `/rating` — сложнее: таблица живёт с фильтрами, сортировкой и экспортом, пластины должны с ними ужиться, а строк 55, не 10.
- ДВА РЕГРЕССА, ПОЙМАННЫХ 08.09 ПРОВЕРКОЙ НА ЖИВОМ САЙТЕ (оба были мои, оба сутки жили на бою): шапка распирала страницу на всех ширинах 1180–1580 px (кнопка «Цветовая тема» с подписью, на 1440 документ 1506 при 1440) — приёмка проверяла только 360/768/1280 и промахивалась; липкий заголовок таблицы не работал вовсе (sticky внутри `.table-scroll` с overflow липнет к обёртке и уезжает с ней, замер top:-503). ВЫВОД НА БУДУЩЕЕ: проверять эффект ПОВЕДЕНИЕМ в браузере, а не наличием объявления в CSS, и мерить ширины между бургером и большим экраном.
- ЛИПКИЕ ЗАГОЛОВКИ ТАБЛИЦ СНЯТЫ НАМЕРЕННО, не возвращать простым `position:sticky` — не работает. Рабочий путь: обёртка с `max-height` и собственной вертикальной прокруткой, тогда шапка липнет к ней. Это меняет поведение страницы — отдельная задача. В `site.css` на месте правила стоит комментарий, приёмка падает, если объявление вернётся.
- ОСТАТОК ПО РАЗВЕДКЕ РТТ (08.09.2026, источники: tennis-russia.ru, rustennistur.com, **rttstat.ru** — там открытый журнал изменений, повторно разведку не гонять): (1) статус приёма заявок на турнир — «подача заявок / поздняя заявка / закрыт», сейчас «Заявки до» просто текст; (2) календарный вид турниров вместо списка; (3) множественный выбор категорий в фильтре календаря; (4) поле «формат турнира» — выходного дня / недельный; (5) сетки парных разрядов с посевом на странице турнира. НЕ делать: избранных игроков с уведомлениями и сохранённые фильтры — нужен полноценный кабинет с подписками, для области на 55 игроков избыточно.
- СУДЬИ, фактура для работы: категории лестницей ЮС → 3К → 2К → 1К → ВК, следующая только при наличии предыдущей, после квалификационного экзамена; 1К бывает у МС/МСМК; категорию ПОДТВЕРЖДАЮТ раз в несколько лет — потому в карточке есть `category_date`. Значки ITF (белый/бронзовый/серебряный/золотой) — не российская категория, пишутся словами. Официальный учёт — карточка учёта судейской деятельности в региональной коллегии судей, сайт даёт только витрину.
- ЖДЁТ ВЛАДЕЛЬЦА: логотипы партнёров (6 файлов готовы, «Алпина» белая — нужен цветной от клуба), фото из MAX (три поста 07.09), даты рождения 103 игроков и имена 20 ветеранов, объединение Коротковых #2 и #21 (кабинет и дата рождения на #2, история чемпионата на #21), публикация 4 черновиков новостей, снятие плашки кнопкой на Сводке.
- ЖДЁТ ФЕДЕРАЦИИ: матч Третьякова — Лобанова у женщин 45+ (доиграть или засчитать неявку — иначе круг открыт и места не пишутся, четыре ветеранки вне рейтинга); основания публикации и даты документов на 16 тренеров ЛИБО их анкеты через сайт; живо ли Сафоново (МЖК) и корты СГУС.
- ОТКРЫТЫЙ ВОПРОС МЕТОДИКИ: в срезах рейтинга по полу места общие (4, 4, 4, 9…), в срезе по разряду — пересчитываются внутри выборки (1, 2, 2…). Привести к одному поведению по решению федерации.
- СРОК: домен ftso67.ru оплачен до **16.11.2026** — продлить у RU-CENTER.
- Правила: палитра не меняется; `git status --porcelain` пуст → ветка; мерж только на зелёной со стоп-условием `grep PROVALENO`; токен `/mnt/user-data/uploads/gh_token_write.txt`.
- Закрытие: пачка в `ritual/`, затем `node /tmp/sandbox/tools/ritual.mjs ftso 15 "…" --clone "$PWD"`.

## ⚡ ВХОД СЛЕДУЮЩЕЙ СЕССИИ — START13_V1

- Бой на `ff47f34`+ (main): приёмка `node acceptance.mjs` из `site/` → **217/217**. Перед словом «на бою» сверять `site.css?v=` боя с локальным md5 (js+css).
- ДАННЫЕ ЗАЛИТЫ 08.09.2026: три турнира опубликованы (#4 чемпионат 13–19.07, #5 первенство до 13/до 17 24–29.08, #6 ветераны 2–7.09), 108 игроков, рейтинг пересчитан — 55 в таблице. Тексты протоколов — `protokoly/` в outputs чата, редакция 2 у ветеранов.
- СВЕРКА РЕЙТИНГА ПРОЙДЕНА 08.09.2026: независимый пересчёт по методике (100/70/45/25/10 × A2/B1/C0,5, 6 лучших за 52 недели) против боевой таблицы — 55 из 55 совпало, расхождений 0. Пункт бэклога «тест движка на реальных данных до go-live» закрыт.
- Сделано за сессию 13 (PR #167–#180): три темы и меню «Цветовая тема», партнёры вариант B, семь эффектов интерфейса (ящик турнира, пластины рейтинга, кнопки, появление секций, подпись фото, пустые состояния), стартовые партнёры и справочники по прежнему сайту Федерации, инструкция Word `tools/build-guide.mjs`.
- ШЕСТЬ ДЕФЕКТОВ, найденных на живых данных и исправленных: микст писался как `double` и затирал парное место (нужна была третья дисциплина); незакрытый `</form>` на Сводке ломал кнопку пересчёта; микст подписывался «одиночный» в таблице мест; пол в миксте схлопывался в мужской; дубль игрока «Фамилия Имя» при «Фамилия Имя Отчество»; обязательное имя запирало карточки из импорта.
- ЖДЁТ ВЛАДЕЛЬЦА: логотипы партнёров (6 файлов готовы, «Алпина» белая — нужен цветной), фото из MAX (три поста 07.09), даты рождения 103 игроков и имена 20 ветеранов, объединение Коротковых #2 и #21, публикация 4 черновиков новостей, снятие плашки кнопкой на Сводке.
- ЖДЁТ ФЕДЕРАЦИИ: матч Третьякова — Лобанова у женщин 45+ (доиграть или неявка, иначе круг не закрыт и места не пишутся), основания и даты документов на 16 тренеров, живо ли Сафоново (МЖК) и корты СГУС.
- РЕШЕНО ВЛАДЕЛЬЦЕМ, НЕ ПЕРЕСПРАШИВАТЬ: телефон на сайте — **8-995-104-32-62** (действующий). Номер +7 904 360-90-90 из договорного ТЗ и реестра ФТР устарел, на сайт не ставить.
- СРОК: домен ftso67.ru оплачен до **16.11.2026** — продлить у RU-CENTER.
- Правила: палитра не меняется; `git status --porcelain` пуст → ветка; мерж только на зелёной со стоп-условием `grep PROVALENO`; токен `/mnt/user-data/uploads/gh_token_write.txt`.
- Закрытие: пачка в `ritual/`, затем `node /tmp/sandbox/tools/ritual.mjs ftso 14 "…" --clone "$PWD"`.

