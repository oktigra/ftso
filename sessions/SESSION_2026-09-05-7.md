# Сессия 7 — 05.09.2026 (Claude-чат): ТЗ по пунктам, решения владельца, ускорение ввода

## Сделано, по замеру (main 90db729, бой = клон, приёмка 178/178)
- **ТЗ 4.3** турниры: город, дата начала, тип (командная встреча / первенство / турнир), возраст; статус от дат; шесть фильтров (PR #80).
- **ТЗ 4.2/4.5/4.6**: поиск по новостям (регистр кириллицы, черновики скрыты); справочники — тренеры (город, клуб, специализация, квалификация, «с кем работает»), корты (город, покрытие, кортов, сезонность, клуб, контакт), клубы (город, карта); фильтры из заполненных значений; карта по адресу (PR #81).
- **ТЗ 4.4**: `/rating.xlsx` — свой OOXML+zip без зависимостей (`server/lib/xlsx.mjs`), `/rating/print` — печатная версия для PDF (PR #82).
- **ТЗ 4.8**: галерея — `<dialog>`, листание, Esc. **ТЗ 8**: кнопка «Написать в MAX» (`MAX_URL`, только `https://max.ru/…`) (PR #83).
- **Метрика**: `METRIKA_ID`, баннер «Принять/Отклонить», счётчик только после согласия, cookie `ftso.analytics` на год, CSP с `mc.yandex.ru` только при счётчике, «Настройки cookie», Политика разд. 8 (код — #84, приёмка — #85).
- **ТЗ п. 11**: инструкция `/admin/guide`, разделы по роли, ссылка в меню (PR #86).
- **Ускорение ввода** п. 4/6/1: datalist игрока, «#id»/город при тёзках, новый игрок из формы результата, матч по тексту, пересчёт со страницы турнира (PR #87); массовый ввод из протокола с предпросмотром и сохранением одной транзакцией (PR #88).
- Ранее в сессии: модель РТТ по согласиям (#67), плашка «до наполнения» кодом (#70), подтверждение опасных кнопок (#73), «Вход» в шапке (#74), временный пароль себе (#75), «Сохранить данные для входа» (#78), дубли игроков (#79), BACKLOG (#84).

## Решения владельца 05.09
- Телефон на сайте — номер из ЕГРЮЛ (см. legal.mjs) (закрыто). MAX — единственный мессенджер. Метрика — да, с баннером. SQLite vs «MySQL/PostgreSQL» в ТЗ — закрыть бумагой (строка в акте), переезд не даёт ничего.
- «Вкладывать в ускорение ввода, а не в базу» — список в BACKLOG, порядок 4 → 1 → 6 → 3 → 2.

## Ждёт владельца (в `.env` на сервере + `pm2 restart ftso`)
- `METRIKA_ID=<номер>` — счётчик на аккаунт федерации. `MAX_URL=https://max.ru/<канал>`.

## Открыто
- Ускорение: п. 3 сетка турнира (после первых турниров через сайт — уточнить форматы), п. 2 импорт xlsx/csv, п. 5.
- Бумага по SQLite (акт/допсоглашение). Документ инструкции для сдачи — из `/admin/guide` при передаче.

## Грабли
- Код Метрики уехал в PR #84 (BACKLOG) — `checkout` с незакоммиченным деревом. Механика: `git status --porcelain` пуст → только тогда `checkout`.
- `git bundle create` в `/mnt/user-data/outputs` падает — собирать в `/tmp`.
- SQLite `LIKE` не знает регистра кириллицы — поиск по новостям в JS.
- `action_log`: фильтр `LIKE '%…%'`, как в соседях, а не `=`.
