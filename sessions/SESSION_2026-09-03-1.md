# Сессия 1 по ФТСО (03.09.2026) — публичный профиль по модели РТТ, и трафарет ритуала

Нумерация ФТСО своя, с единицы: домик WGR (`claude-house`) про этот проект не знает
(грепом по 97 сессиям — 9 упоминаний, все строчкой), память ФТСО — этот репозиторий.

## Сделано (числа из замеров)

- **PR #9** (`2d33498` + `421f98d`) смёржен в `ab33c7b`, автовыкат прошёл: css на бою
  md5 = локальному, `/rating` 200, `/rating?slice=u15` 200, `/player/abc` 404.
  `/player/1` 404 — база боя пуста (рейтинг 0 строк, `/tournaments/1` 404).
- Приёмка на клоне **139/139** (было 130): переписаны 4 проверки старой модели,
  добавлен раздел 19 (§8.1–8.9 ТЗ + админ-фото + счёт/дата + обезличенный) и
  браузерный стенд на профиль (фото под CSP, «злое» имя — текст, alert 0, адаптив
  360/768, доступность).
- Что в коде: `/player/:id` и `/player/:id/photo` (inline, no-cache + ETag/304);
  возраст в полных годах и срезы РТТ в одном месте `site/server/lib/age.mjs`; парный
  разряд в снимке и фильтре; `anonymizeForPublic` знает только «Игрок удалён» —
  согласие и 18-летие на витрину не влияют; `adulthood.mjs` отзыв в 18 не пишет;
  кабинет — подсказка §7, `POST /cabinet/photo/delete`; админка —
  `POST /admin/players/:id/photo/delete`, поля счёт/дата матча. Схема: `players.rni`,
  `results.discipline`, `matches.score/played_on/kind/*_partner_id`, две таблицы
  пересобраны миграцией (идемпотентна, проверена дважды на базе прежней схемы).
- `COMPLIANCE.md` (сноска ред. 6, 4.8 отменён), `BACKLOG.md` (блок 03.09),
  `README.md` — под новую модель.
- **PR #10** (`3314ffd`): трафарет ритуала `tools/ritual.mjs` + `NEXT_CHAT_START.md`.
  Сухой прогон на копии: 5 сторожей из 5 сработали как ожидалось.

## Что проверено руками владельца

`/root/deploy-A.sh` на бою: строка 55 `npm run migrate` с откатом, строка 65 —
проверка 200 после рестарта, но только по `/`. Новые страницы выкат не проверяет.

## Открыто

1. Правило возраста для среза: полные годы на сегодня (как в ТЗ) или год рождения
   (календарь РТТ) — одна функция `ageOn`. Решение федерации.
2. Кнопка «согласие на распространение» в кабинете и второй чекбокс регистрации на
   витрину не влияют (фото — по действию). Оставлены ради журнала; снести отдельным PR.
3. Тексты Политики и Согласия (раздел 3 ТЗ) — чат «Политика/фото»; код ссылается на
   п. 2.7 Согласия в новой формулировке, которой в опубликованной редакции ещё нет.
4. Набор срезов под региональный масштаб — список в `age.mjs`, согласовать с ФТСО.
5. Ревью отдельной головой из песочницы недоступно (нет ключа API) — заменено стендом.

## Слепые места этой сессии

- Вошёл по памяти о домике, а не по домику: память про `/areas/ftso-*.md` ложная.
- Выходил из хода вопросами («скажи "продолжай"», «могу собрать пачку») — нельзя.
- Дал команду с угаданными путями на сервере — отозвал.
- Пять минут мерил выкат по `/css/site.css` (страница 404): путь верный —
  `/static/css/site.css`, из `href` в HTML.
- В памяти чата индекс оператора 214036 — верный 214020, в `legal.mjs` верно.
