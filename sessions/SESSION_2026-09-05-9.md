# Сессия 9 — 05.09.2026 (Claude-чат): сетка турнира тремя слоями, MAX, инструкция в Word

## Сделано, по замеру (main 1fe353a, бой = клон, приёмка 188/188)
- Сетка, слой 1 — круговые группы (#104): `tournament_groups`/`tournament_group_members`, таблица N×N, счёт в клетке от строки (сеты, тай-брейк, w/o), победитель по сетам, места победы → личная встреча → сеты → геймы, «Записать места» при полной группе. `server/lib/groups.mjs`.
- Слой 2 — олимпийка 4/8/16/32 (#105): `tournament_brackets`/`bracket_slots`, посев по позициям, bye, итог пары от верхнего, отмена с откатом дальше, места 1/2/3/5/9/17. `server/lib/brackets.mjs`.
- Слой 3 — группы + плей-офф (#106): посев из мест в группах (первые против вторых других групп, стандартный порядок посева `seedOrder`), места «сетка + группы» (не вышедшие — корзина за сеткой).
- Витрина турнира показывает группы и сетку; инструкция дополнена (#107). Матчи из сеток — обычные `matches`.
- MAX: `https://max.ru/channel_ftso` умолчанием в `config.mjs` (#103), на бою кнопка есть (замер). SQLite-бумага исключена решением владельца.
- Инструкция по администрированию — Word для сдачи (в outputs чата), контакты: ИП Коротков Олег Александрович, телефон владельца (в репо не кладётся).
- ТЗ браузерному агенту по счётчику Метрики — HTML-артефакт (в outputs чата).

## Ждёт владельца
- `METRIKA_ID` из ответа браузерного агента → `.env` + `pm2 restart ftso` (или прислать — зашью умолчанием).
- Наполнение сайта (делает сам).

## Открыто (код)
- Ничего по ТЗ. Возможные улучшения — только по слову владельца.

## Грабли
- Посев из групп «змейкой» сводил одногруппников в первой паре; со стандартным порядком посева правильный порядок сеяных — все первые места, затем все вторые, без реверса.
- Переворот счёта: тай-брейк в скобках остаётся при сете («6:7(5)» → «7:6(5)»); в тестах писать ожидания по факту.
