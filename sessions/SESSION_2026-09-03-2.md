# Сессия 2 по ФТСО (03.09.2026, хвост сессии 1) — трафарет ритуала стал общим

Снимок 1 закрывал выкат профиля. После него в той же сессии сделано ещё, и память
должна это знать — иначе следующий чат пойдёт по устаревшей строке из блока START1.

## Сделано (числа из замеров)

- **Трафарет ритуала — один на все проекты**: `oktigra/orchestrator-sandbox`
  `tools/ritual.mjs` (RITUAL_TEMPLATE_V1) + описания `tools/ritual/ftso.json` (mode
  merge) и `tools/ritual/webguardreport.json` (mode letter). Новый проект = новый json.
  Песочница main `0e0efc3`.
- Сухие прогоны: ftso merge — два файла памяти, блоки 2 и 1, md5 = md5sum; WGR letter
  на живом файле входа домика — 5 файлов, 11 меток, якоря START105 / START104 /
  комментарий 98, md5 манифеста = md5sum пачки. Тестовое письмо 106 в песочницу не
  ушло — стёрто до коммита.
- В `ftso`: локальная копия `tools/ritual.mjs` убрана (PR #13 `fccafd0`),
  `NEXT_CHAT_START.md` ведёт к трафарету. Ошибка первого прогона (md5 по обрезанному
  тексту, `.trim()` в помощнике) закрыта PR #12. Ветки после мержа удаляются самим
  скриптом; старые слитые ветки этого дня с GitHub сняты.
- Бой при трёх мержах без кода сайта не менялся: `/` 200, `/rating` 200, css md5
  прежний — только пустые рестарты pm2.

## Открыто

1. Домик WGR о трафарете не знает: записи туда у чата нет. Первое живое письмо
   `webguardreport 106` через `ritual.mjs` и будет проверкой mode letter на машине —
   в его блок входа положить строку о трафарете.
2. Автовыкат `ftso` перезапускает pm2 и на мержах одной памяти; если это начнёт
   мешать — в `deploy-watch.sh` пропуск диффов, где только `sessions/`, `ritual/`,
   `NEXT_CHAT_START.md`. Не делал: рестарт пустой и быстрый.
3. Всё открытое из снимка 1 (правило возраста для среза, кнопка согласия на
   распространение, тексты Политики/Согласия, набор срезов) — без изменений.

## Слепые места хвоста

- Первый трафарет сделал под один проект; общий — со второго захода, после
  разъяснения. Читать вопрос до конца, а не первое предложение.
- Пуш в песочницу упёрся в свежий коммит моста и в неустановленную подпись git —
  два холостых захода; теперь подпись ставится в клоне до первого коммита.
